---
- name: Implementing tags
  hosts: all
  become: yes
  gather_facts: false
  tasks:
    - name: installing tree
      apt:
       name: tree
       state: present
       update_cache: yes
      tags: tree_installation
    - name: create user
      user: 
       name: Rakesh
       password: 123456
      tags: user_creation
    - name: copy file
      copy:
        src: /etc/passwd
        dest: /tmp

...
