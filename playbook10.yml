---
- name: Implementing hotscope variables
  hosts: 172.31.19.43
  tasks:
    - name: creating a user
      user:
        name: "{{a}}"
        password: "{{b}}"
        uid: "{{c}}"
        home: "{{d}}"
        shell: "{{e}}"
        comment: "{{f}}"

...
